name: Codex report PDF persistence

on:
  push:
    branches:
      - main
    paths:
      - ".codex-report-pdf-persistence-trigger"

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply report PDF persistence patch
        run: python .github/scripts/codex_patch_report_pdf_persistence.py
      - name: Verify JavaScript
        run: node --check assets/scripts/app.js
      - name: Commit patch
        run: |
          git config user.name "codex-app[bot]"
          git config user.email "codex-app[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet || git commit -m "Keep uploaded report PDFs visible"
          git push
