name: Codex report visibility

on:
  push:
    branches:
      - main
    paths:
      - ".codex-report-visibility-trigger"

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply report visibility patch
        run: python .github/scripts/codex_patch_report_visibility.py
      - name: Verify JavaScript
        run: node --check assets/scripts/app.js
      - name: Commit patch
        run: |
          git config user.name "codex-app[bot]"
          git config user.email "codex-app[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet || git commit -m "Show saved reports immediately"
          git push
